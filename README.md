# openenv-task-manager
This project implements a custom OpenEnv-compatible reinforcement learning environment using FastAPI and Docker. It exposes reset and step APIs over HTTP, simulating task management workflows with reward-based actions, enabling real-world agent interaction and scalable deployment.
